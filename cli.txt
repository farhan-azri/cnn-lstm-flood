
python Chapter\ 4/cnn-lstm-confustion-matrix.py

deactivate 2>/dev/null || true

rm -rf venv

python3 -m venv venv

source venv/bin/activate

pip install -U pip setuptools wheel

pip install -r requirements.txt


python main.py

streamlit run streamlit_app.py